{
  "name": "dsh-plugin-admin",
  "version": "0.6.0",
  "type": "module",
  "description": "dsh web UI admin plugin: extension-plugin marketplace tab, MCP server & session-history settings pages, subagent management (named delegation-tool instances with persona/tool-filter/model config, plus external CLI backend mounting), and slash-command & Claude-Code-format hook administration (file-backed live commands, hooks.json + disable sidecar, stock hooks-bridge install/uninstall) — all persisted as profile cordis.patch.yml rows or dsh-home JSON that Cordis HMR / live registration hot-applies",
  "main": "lib/index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zerosloney/dsh-plugin-admin.git"
  },
  "keywords": [
    "dsh",
    "dsh-plugin",
    "admin",
    "subagent",
    "mcp",
    "session",
    "commands",
    "hooks",
    "cordis"
  ],
  "scripts": {
    "test": "node scripts/self-check.mjs && node scripts/host-check.mjs && node scripts/verify-mcp-cache.mjs && node scripts/verify-update-reminders.mjs && node scripts/verify-subagents-host.mjs && node scripts/verify-subagents-client.mjs && node scripts/verify-command-hooks.mjs"
  },
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
